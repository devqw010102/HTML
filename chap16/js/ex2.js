let width;      // 너비
let height;     // 높이

width = 200;
height = 50;

let area = width * height;
console.log(area);

width = 100;
height = 200;

area = width * height;
console.log(area);          // 변수 값 변경 가능

const constWidth = 200;
// constWidth = 2;           상수 변경 불가

